# Procurement Traceability

## Purchase Request Management
**Chain:** Purchase Request Management → PR Creation & Approval → PurchaseRequestForm, PurchaseRequestList, PurchaseRequestDetail, PRAuthorizationWorkflow → POST /purchase-requests, GET /purchase-requests, GET /purchase-requests/:id, PUT /purchase-requests/:id, POST /purchase-requests/:id/approve, POST /purchase-requests/:id/reject → PurchaseRequest, PurchaseRequestLineItem, PurchaseRequestApproval, PurchaseRequestBudgetCheck → PRBudgetCheckRule, PRAuthorizationMatrixRule, PRUrgencyRule, PRCategoryRule → PurchaseRequests.Create, PurchaseRequests.View, PurchaseRequests.Edit, PurchaseRequests.Delete, PurchaseRequests.Approve, PurchaseRequests.Reject → PRSubmittedNotification, PRAssignedForApprovalNotification, PRApprovedNotification, PRRejectedNotification → PRPipelineReport, PRAgingReport, PRBudgetUtilizationReport → ProcurementDashboard → PRAutoApprovalAI
**Status:** ✅ Full Trace

## RFQ Management
**Chain:** RFQ Management → Request for Quotation & Supplier Response → RFQForm, RFQList, RFQDetail, RFQResponseView, SupplierComparisonView → POST /rfqs, GET /rfqs, GET /rfqs/:id, PUT /rfqs/:id, POST /rfqs/:id/send, POST /rfqs/:id/responses, POST /rfqs/:id/evaluate → RFQ, RFQLineItem, RFQResponse, RFQEvaluation, RFQSupplier → RFQDeadlineRule, RFQAutoSendRule, RFQEvaluationCriteriaRule, RFQMinimumBidderRule → RFQs.Create, RFQs.View, RFQs.Edit, RFQs.Delete, RFQs.Evaluate → RFQCreatedNotification, RFQResponseReceivedNotification, RFQDeadlineReminderNotification → RFQResponseComparisonReport, RFQEvaluationReport, RFQCycleTimeReport → ProcurementDashboard → SupplierRecommendationAI, BidEvaluationAI
**Status:** ✅ Full Trace

## Purchase Order Management
**Chain:** Purchase Order Management → PO Creation & Fulfillment Tracking → POForm, POList, PODetail, POReceivingView, POStatusTracker → POST /purchase-orders, GET /purchase-orders, GET /purchase-orders/:id, PUT /purchase-orders/:id, POST /purchase-orders/:id/approve, POST /purchase-orders/:id/issue, POST /purchase-orders/:id/close → PurchaseOrder, PurchaseOrderLineItem, PurchaseOrderRevision, PurchaseOrderApproval, PurchaseOrderReceipt → POBudgetReservationRule, POAuthorizationMatrixRule, PORevisionControlRule, POClosureValidationRule → PurchaseOrders.Create, PurchaseOrders.View, PurchaseOrders.Edit, PurchaseOrders.Delete, PurchaseOrders.Approve, PurchaseOrders.Close → POCreatedNotification, POApprovedNotification, POIssuedToSupplierNotification, POPartiallyReceivedNotification, PODueForDeliveryNotification → POPipelineReport, POBudgetConsumptionReport, PODeliveryPerformanceReport → ProcurementDashboard → PODeliveryPredictionAI
**Status:** ✅ Full Trace

## Goods Receipt Management
**Chain:** Goods Receipt Management → Receiving & Inspection → GoodsReceiptForm, GoodsReceiptList, GoodsReceiptDetail, InspectionResultForm, ReceivingDashboard → POST /goods-receipts, GET /goods-receipts, GET /goods-receipts/:id, PUT /goods-receipts/:id, POST /goods-receipts/:id/inspect, POST /goods-receipts/:id/confirm → GoodsReceipt, GoodsReceiptLineItem, InspectionResult, NonConformanceRecord → QuantityMatchRule, QualityInspectionRule, POReleaseRule, OverReceiptToleranceRule → GoodsReceipts.Create, GoodsReceipts.View, GoodsReceipts.Edit, GoodsReceipts.Inspect, GoodsReceipts.Confirm → GoodsReceiptCreatedNotification, InspectionRequiredNotification, NonConformanceNotification, GoodsReceiptConfirmedNotification → GoodsReceiptSummaryReport, InspectionPassFailReport, SupplierQualityReport → QualityDashboard, ProcurementDashboard → InspectionAutoSchedulingAI
**Status:** ✅ Full Trace

## Returns Management
**Chain:** Returns Management → Supplier Returns & Credit Processing → ReturnForm, ReturnList, ReturnDetail, ReturnApprovalWorkflow, CreditNoteView → POST /returns, GET /returns, GET /returns/:id, PUT /returns/:id, POST /returns/:id/approve, POST /returns/:id/ship → ReturnOrder, ReturnOrderLineItem, ReturnReason, ReturnShipment, CreditNote → ReturnAuthorizationRule, ReturnWindowRule, ReturnRestockingFeeRule, CreditNoteGenerationRule → Returns.Create, Returns.View, Returns.Edit, Returns.Delete, Returns.Approve → ReturnCreatedNotification, ReturnApprovedNotification, ReturnShippedNotification, CreditNoteIssuedNotification → ReturnSummaryReport, ReturnByReasonReport, SupplierReturnRateReport → ProcurementDashboard, QualityDashboard → ReturnReasonClassificationAI
**Status:** ✅ Full Trace

## Supplier Negotiation
**Chain:** Supplier Negotiation → Negotiation Planning & Execution → NegotiationForm, NegotiationList, NegotiationDetail, NegotiationScorecard, ContractTermView → POST /negotiations, GET /negotiations, GET /negotiations/:id, PUT /negotiations/:id, POST /negotiations/:id/scorecard, POST /negotiations/:id/close → Negotiation, NegotiationRound, NegotiationTerm, NegotiationScorecard, NegotiationDocument → NegotiationConfidentialityRule, ScoringRule, ApprovalThresholdRule → Negotiations.Create, Negotiations.View, Negotiations.Edit, Negotiations.Delete, Negotiations.Close → NegotiationScheduledNotification, NegotiationCompletedNotification → NegotiationOutcomeReport, SavingsReport, SupplierComparisonReport → ProcurementDashboard → NegotiationInsightAI
**Status:** ✅ Full Trace
