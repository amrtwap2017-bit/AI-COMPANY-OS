# Human Resources Traceability

## Employee Management
**Chain:** Employee Management → Employee Lifecycle & Records → EmployeeForm, EmployeeList, EmployeeDetail, OrganizationChartView, EmployeeSelfServiceView → POST /employees, GET /employees, GET /employees/:id, PUT /employees/:id, POST /employees/:id/onboard, POST /employees/:id/offboard → Employee, EmployeeDocument, EmployeeContact, EmploymentHistory, EmployeeStatus → EmployeeOnboardingRule, EmployeeOffboardingRule, OrganizationChartRule, EmployeeStatusTransitionRule → Employees.Create, Employees.View, Employees.Edit, Employees.Delete, Employees.Onboard, Employees.Offboard → EmployeeOnboardedNotification, EmployeeOffboardedNotification, EmployeeBirthdayNotification, WorkAnniversaryNotification → EmployeeHeadcountReport, EmployeeTurnoverReport, OrgChartReport, EmployeeDemographicsReport → HRDashboard → EmployeeTurnoverPredictionAI
**Status:** ✅ Full Trace

## Department Management
**Chain:** Department Management → Organizational Structure & Hierarchy → DepartmentForm, DepartmentList, DepartmentDetail, DepartmentHierarchyView, CostCenterAssignmentView → POST /departments, GET /departments, GET /departments/:id, PUT /departments/:id, POST /departments/:id/assign-manager, POST /departments/:id/assign-cost-center → Department, DepartmentBudget, DepartmentHead, DepartmentHierarchy, CostCenterAllocation → DepartmentHierarchyRule, BudgetAllocationRule, ManagerAssignmentRule, DepartmentRestructureRule → Departments.Create, Departments.View, Departments.Edit, Departments.Delete → DepartmentRestructuredNotification, ManagerAssignedNotification → DepartmentStructureReport, DepartmentBudgetReport, CostAllocationReport → HRDashboard → DepartmentOptimizationAI
**Status:** ✅ Full Trace

## Leave Management
**Chain:** Leave Management → Leave Request & Approval → LeaveRequestForm, LeaveCalendarView, LeaveBalanceView, LeaveApprovalWorkflow, TeamLeaveView → POST /leaves, GET /leaves, GET /leaves/:id, PUT /leaves/:id, POST /leaves/:id/approve, POST /leaves/:id/reject, GET /leaves/balance → LeaveRequest, LeaveType, LeaveBalance, LeaveApproval, LeavePolicy → LeaveAccrualRule, LeavePolicyRule, ConflictDetectionRule, CarryForwardRule, ApprovalHierarchyRule → Leaves.Create, Leaves.View, Leaves.Edit, Leaves.Delete, Leaves.Approve, Leaves.Reject → LeaveRequestSubmittedNotification, LeaveApprovedNotification, LeaveRejectedNotification, LeaveBalanceLowNotification, TeamMemberOnLeaveNotification → LeaveSummaryReport, LeaveBalanceReport, LeaveUtilizationReport, LeaveForecastReport → HRDashboard → LeaveForecastAI
**Status:** ✅ Full Trace

## Timesheet Management
**Chain:** Timesheet Management → Time Entry & Approval → TimesheetForm, TimesheetListView, TimesheetDetail, TimesheetApprovalView, WeeklySummaryView → POST /timesheets, GET /timesheets, GET /timesheets/:id, PUT /timesheets/:id, POST /timesheets/:id/submit, POST /timesheets/:id/approve, GET /timesheets/approval-pending → Timesheet, TimesheetEntry, TimesheetApproval, TimeCategory, BillableRate → TimesheetValidationRule, OvertimeRule, BillableHourRule, WeeklyLimitRule, ApprovalDeadlineRule → Timesheets.Create, Timesheets.View, Timesheets.Edit, Timesheets.Delete, Timesheets.Submit, Timesheets.Approve → TimesheetReminderNotification, TimesheetSubmittedNotification, TimesheetApprovedNotification, TimesheetRejectedNotification, OvertimeAlertNotification → TimesheetSummaryReport, BillableHoursReport, OvertimeAnalysisReport, UtilizationReport → HRDashboard, ProjectDashboard → TimesheetAnomalyDetectionAI
**Status:** ✅ Full Trace

## Attendance Management
**Chain:** Attendance Management → Clock-In/Out & Attendance Tracking → AttendanceClockView, AttendanceLogView, AttendanceSummaryView, ExceptionView, ShiftScheduleView → POST /attendance/clock-in, POST /attendance/clock-out, GET /attendance, GET /attendance/:employeeId, GET /attendance/exceptions, POST /attendance/shifts → AttendanceRecord, ShiftSchedule, AttendanceException, GeolocationLog, AttendanceSummary → AttendancePolicyRule, LateThresholdRule, EarlyDepartureRule, OvertimeCalculationRule, GracePeriodRule → Attendance.ClockIn, Attendance.ClockOut, Attendance.View, Attendance.Override, Attendance.Configure → LateArrivalNotification, EarlyDepartureNotification, AbsentNotification, AttendanceExceptionNotification → AttendanceSummaryReport, PunctualityReport, AbsenteeismReport, ShiftCoverageReport → HRDashboard → AttendancePatternAI
**Status:** ✅ Full Trace

## Payroll Management
**Chain:** Payroll Management → Salary Processing & Compliance → PayrollForm, PayrollListView, PayrollDetail, PaySlipView, TaxFormView → POST /payroll, GET /payroll, GET /payroll/:id, PUT /payroll/:id, POST /payroll/:id/process, POST /payroll/:id/release, GET /payroll/:id/payslip → PayrollRun, PayrollLineItem, PaySlip, TaxDeclaration, Deduction → SalaryCalculationRule, TaxWithholdingRule, DeductionRule, OvertimeCalculationRule, StatutoryComplianceRule → Payroll.Create, Payroll.View, Payroll.Edit, Payroll.Process, Payroll.Release, Payroll.Configure → PayrollProcessedNotification, PayrollReleasedNotification, PaySlipAvailableNotification, TaxFilingReminderNotification → PayrollSummaryReport, SalaryStructureReport, TaxDeductionReport, PayrollCostReport → HRDashboard, FinancialDashboard → PayrollAnomalyDetectionAI
**Status:** ✅ Full Trace

## Recruitment Management
**Chain:** Recruitment Management → Hiring Pipeline & Onboarding → JobPostingForm, CandidateListView, CandidateDetailView, InterviewScheduleView, OfferLetterView → POST /recruitment/jobs, GET /recruitment/jobs, GET /recruitment/jobs/:id, POST /recruitment/candidates, GET /recruitment/candidates, PUT /recruitment/candidates/:id, POST /recruitment/candidates/:id/offer → JobPosting, Candidate, Application, Interview, OfferLetter → JobApprovalRule, CandidateScreeningRule, InterviewScoreRule, OfferApprovalRule → Recruitment.Jobs.Create, Recruitment.Jobs.View, Recruitment.Candidates.View, Recruitment.Candidates.Screen, Recruitment.Offers.Create → NewApplicationNotification, InterviewScheduledNotification, OfferSentNotification, OfferAcceptedNotification → RecruitmentFunnelReport, TimeToHireReport, SourceEffectivenessReport, HiringCostReport → HRDashboard → CandidateMatchingAI, ResumeParsingAI
**Status:** ✅ Full Trace

## Training & Development
**Chain:** Training Management → Course Tracking & Skill Development → CourseForm, CourseCatalogView, EnrollmentView, TrainingRecordView, SkillMatrixView → POST /training/courses, GET /training/courses, GET /training/courses/:id, POST /training/enroll, GET /training/enrollments, POST /training/complete → TrainingCourse, TrainingEnrollment, TrainingRecord, Skill, Certification → EnrollmentEligibilityRule, CourseCompletionRule, CertificationExpiryRule, SkillEndorsementRule → Training.Courses.Create, Training.Courses.View, Training.Enroll, Training.Record → EnrollmentConfirmationNotification, CourseReminderNotification, CertificationExpiryNotification, TrainingCompletedNotification → TrainingCompletionReport, SkillGapAnalysisReport, CertificationStatusReport → HRDashboard → LearningRecommendationAI
**Status:** ✅ Full Trace
