id: tenant
name: "Tenant"
status: Partial
source_paths:
  - core/tenant.py
  - commercial/hotels
aggregates: [Organization,Site,Membership,Entitlement]
entities: [Organization,Site,Membership,Entitlement]
value_objects: [Identifier, Money, DateRange, Status, TenantScope]
repositories: [tenant_repository]
use_cases: [create, update, retrieve, search, approve, complete]
policies: [tenant_scope, authorization, lifecycle, audit]
events: [TenantCreated, TenantUpdated]
commands: [CreateTenant, UpdateTenant, CompleteTenant]
queries: [GetTenant, SearchTenant]
read_models: [tenant_summary, tenant_timeline]
write_models: [tenant_aggregate]
external_dependencies: [identity, tenant, configuration, documents, notifications]
migration: adapter_then_vertical_slice
compatibility: preserve_current_routes_models_and_imports

