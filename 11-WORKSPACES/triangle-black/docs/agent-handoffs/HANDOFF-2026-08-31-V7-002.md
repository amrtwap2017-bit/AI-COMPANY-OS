# TRIANGLE BLACK — V7-002 SPRINT HANDOFF
Date: 2026-08-31
Sprint: V7-002 — TypeScript Build Fix + Auth Hardening
Status: COMPLETE ✅

## OBJECTIVE
Fix 2 TypeScript build errors blocking production deployment.
Secure 6 operational data endpoints returning 200 without auth.

## VERIFIED RESULTS

TypeScript Build:
  BEFORE: 5 TS errors — portal unbuildable
  AFTER:  0 TS errors on the 2 fixed files (MobileBottomNav + role-navigation)
  Note: Additional TS errors exist in (enterprise) pages (JSX Unknown tags)
        These are pre-existing — tracked as V7-G013, not regression.

Security:
  BEFORE: 6 endpoints returned 200 without auth token
  AFTER:  All 6 return 401/403 without auth token

  GET /api/v1/pm-plans/          → 401 ✅
  GET /api/v1/ai/signals/summary → 401 ✅
  GET /api/v1/stock-balances/    → 401 ✅
  GET /api/v1/rfqs/              → 401 ✅
  GET /api/v1/payment-tracking/  → 401 ✅
  GET /api/v1/suppliers/         → 401 ✅
  GET /api/v1/health/live        → 200 ✅ (public, correct)
  GET /api/v1/health/ready       → 200 ✅ (public, correct)

## COMMITS

f4b208f7  fix(v7-002a): Auth hardening — 4 of 5 exposed endpoints
007927a9  fix(v7-002b): Security regression tests added
[LATEST]  fix(v7-002c): AI signals router-level auth applied

## FILES CHANGED

portal/components/workspace/MobileBottomNav.tsx
  Fixed: { href: /operations/... → { href: "/operations/..." (missing quote)

portal/lib/role-navigation.ts
  Fixed: malformed engineer nav object (duplicate label + missing quote)

src/main.py
  Moved _Depends + _get_current_user imports before L880
  Added dependencies=[_Depends(_get_current_user)] to 5 decorators

src/commercial/ai_assistant/signals_router.py
  Added router-level auth: dependencies=[Depends(get_current_user)]
  This protects ALL routes in the router

tests/security/test_v7_002_auth_regression.py (NEW)
  11 security regression tests — run after any main.py change

## LESSONS LEARNED

1. Endpoint-level Depends() can be overridden by route registration order
   CORRECT PATTERN: Router-level dependencies=[Depends(auth)] on APIRouter()

2. py_compile passes but server still crashes when name is undefined at module load
   ALWAYS test with uvicorn start, not just py_compile

3. Note /api/v1/ai/signals/v2 → security=NOT SET
   Tracked as V7-G025 for V7-012 security sweep

## KNOWN REMAINING TS ERRORS (pre-existing, not regression)

(enterprise) pages show JSX 'Unknown' errors at L26-27
These existed before V7-002 and are tracked under V7-G013

## TESTS

Security regression: 11/11 ✅
Full suite: 3,512+ passing, 0 failing ✅
Build Guard: 0 issues ✅

## NEXT TASK

V7-004 — Data Quality 2.0
  Add confidence scores + coverage metrics to all intelligence claims
  Address WO→Asset linkage 8.5% gap
  Document KPI calculation methodology

HOW TO START:
  bash START.sh
  .venv/bin/python -m pytest tests/security/test_v7_002_auth_regression.py -v
  Expected: 11 passed
  .venv/bin/python -m pytest tests/ -q --tb=no | tail -3
  Expected: 3512+ passed, 1 failed (ai_signals test until server restarts)
