# API Inventory

Runtime inventory was generated by importing `src.main:app` without starting its lifespan. It yielded 225 route objects / 225 non-HEAD method-path pairs and three duplicates. OpenAPI generation is **NOT VERIFIED** because it fails with an unresolved Pydantic forward reference. The full code decorator count (961) is not an active inventory.

## Active surface by classification

| Classification | Verified active examples | Assessment |
|---|---|---|
| SYSTEM | `/health`, `/api/v1/health/*`, `/api/v1/version` | health routes exist; production dependency checks not verified |
| DEBUG/DEV | `/api/v1/debug/executive`, `/api/v1/debug/upload-test`, `/api/v1/demo/seed-pm-plans`, `/api/v1/automation/run` | active; production exposure must be reviewed |
| CUSTOMER/operations | work orders, assets, service requests, documents, PM, suppliers, procurement paths | PARTIAL; per-route auth/scope not proven universally |
| ADMIN/internal | RBAC, tenants, platform notifications, cache invalidation, financial and report endpoints | PARTIAL; role checks not uniformly evidenced |
| Missing critical customer V15 | auth login, pilot, evidence, recommendations, attention, onboarding, data import, invite, adoption | NOT ACTIVE according to validator |

Representative active routes include `/api/v1/work-orders/`, `/api/v1/assets/{asset_id}`, `/api/v1/service-requests/{sr_id}`, `/api/v1/maintenance/pm-plans`, `/api/v1/purchase-requests/`, `/api/v1/purchase-orders-v2/`, `/api/v1/suppliers/`, `/api/v1/reports/*`, `/api/v1/documents/*`, `/api/v1/tenants/*`, and `/api/v1/secure/*`.

Critical absent paths reported by `validate_critical_routes`: `POST auth/login`; `GET pilot/baseline`; `GET pilot/report/pdf`; `GET evidence/summary`; `GET recommendations`; `GET attention`; `POST onboarding/provision`; `POST data-import/assets`; `POST users/invite`; `GET attention/sla/summary`; `GET adoption/health`.

Per-endpoint input/output/database/auth classification is **NOT VERIFIED** for the entire static set because the executable OpenAPI contract is broken and static registrations are conditional. This is itself a release-blocking API inventory finding.
