# 07 Startup Audit

`startup_validation` is automatically invoked during `src.main` import, so the historical claim it is unwired is CONTRADICTED. It is non-fatal and wrapped: runtime logs 11 missing critical routes and continues. A grouped router registration references undefined `attention_sla_router`, causing a silent skip of V15 routers. Broken routes can thus yield 404 while startup succeeds.
