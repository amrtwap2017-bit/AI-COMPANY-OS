# 27 Test Quality

Many tests are source/HTTP fixture tests with hardcoded local credentials and live-http exclusions. Tests exist for routers that individually import but do not catch the grouped runtime registration defect. CI permits lint/security failures and up to 50 TS errors. Missing proof: deployed runtime contract, data firewall all-path tests, real tenant object authorization, immutable baseline, delivery/retry, restore drill, real customer lifecycle.
