# 16 Pilot Baseline

Pilot source recalculates baseline from live data every request; immutable snapshots and versioning were not verified. Critical pilot routes are absent at runtime. Baseline comparison cannot be accepted as reliable customer proof. FAIL/PARTIAL.
