# 48 Execution Plan

## NOW — P0

P0-01 repair router registration and make validation fatal: evidence `src/main.py` grouped undefined `attention_sla_router`; verify import route count/critical paths; accept no missing critical route/OpenAPI error. P0-02 enforce JWT-bound tenant and mandatory `CustomerDataScope` on every customer read; verify cross-tenant/provenance negative tests. P0-03 remove/rotate tracked development credentials and enforce CI failure scans; verify redacted scanner. P0-04 repair OpenAPI/TypeScript build gates; verify `openapi()` and `tsc --noEmit` zero errors.

## NEXT — P1

P1-01 execute real isolated onboarding/invite/import rehearsal. P1-02 migrate evidence schema and immutable baseline snapshots; verify L3/L4 approval and comparisons. P1-03 persist adoption events from approval/outcome. P1-04 deliver monitoring, backup/isolated restore, staging smoke/Playwright evidence.

## AFTER FIRST CUSTOMER — P2

Consolidate entrypoints/docs, harden contracts/lifecycle/object authorization from observed usage.

## FUTURE — P3

Scale, advanced graph/ML/LLM intelligence only after trustworthy customer data/outcomes.
