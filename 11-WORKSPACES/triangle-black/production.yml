version: "3.9"
services:
  backend:
    build: .
    ports: ["8030:8030"]
    env_file: .env.production
  postgres:
    image: postgres:15
    volumes: [pgdata:/var/lib/postgresql/data]
  redis:
    image: redis:7-alpine
volumes:
  pgdata:
