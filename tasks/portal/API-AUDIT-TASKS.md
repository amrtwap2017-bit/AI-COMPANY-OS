# TRIANGLE BLACK - API ENDPOINT AUDIT AND FIX TASK REGISTRY
# Generated: 2026-07-20 15:29
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
# Source: Browser network tab evidence + direct API testing

=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=
CONFIRMED BUGS FROM BROWSER NETWORK TAB
=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=

BUG-001: /api/v1/notifications?limit=20 -> 500 Internal Server Error
  Root cause: alias route we added crashes (missing parameters)
  Fix: remove broken alias, make backend accept no-trailing-slash via FastAPI

BUG-002: /api/v1/inventory/warehouses/ -> 308 Permanent Redirect
  Root cause: Next.js proxy rewrites redirect trailing-slash URLs
  The next.config.ts rewrite rule /api/v1/:path* catches the path
  but the portal itself 308-redirects the trailing slash before proxy runs
  Fix: Portal pages must call without trailing slash, backend must handle both

BUG-003: http://localhost:8030/api/v1/commercial/invoices -> 404
  Root cause: safe-api.ts is STILL calling localhost:8030 directly
  The fix we applied was correct but some code path imports safe-api
  before our fix and the old URL is cached in the built chunk
  Fix: Complete rebuild after fixing safe-api base URL

BUG-004: T.filter is not a function
  Root cause: some component passes API response dict to useSearch
  useSearch receives {filter:[], data:[]} instead of []
  Fix: our toArray() fix in useSearch must handle ALL dict shapes

=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=
ROOT CAUSE ANALYSIS
=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=

There are actually TWO separate problems fighting each other:

PROBLEM A: Trailing slash stripping
  Next.js proxy: /api/v1/* -> http://localhost:8030/api/v1/*
  When URL is /api/v1/warehouses/ Next.js 308-redirects to /api/v1/warehouses
  Then the redirect hits the proxy correctly
  But fetch() does NOT follow redirects by default (returns 308 body)
  Solution: pages must call WITHOUT trailing slash OR
  add redirect: follow to every fetch call

PROBLEM B: safe-api.ts calls localhost:8030 directly
  When API_URL = http://localhost:8030 the calls bypass the Next.js proxy
  They hit TB Admin directly with CORS + no cookie auth
  Solution: API_URL must be empty string (relative URL through proxy)

PROBLEM C: Broken alias routes in backend
  We added def list_*_root() aliases but they have wrong signatures
  They call list_*() without passing all required parameters
  This causes 500 on the alias routes
  Solution: Use FastAPI include_in_schema + proper parameter passing

=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=
EXECUTION PLAN - FIX ORDER
=

STEP 1: Fix safe-api.ts base URL (must be empty string)
STEP 2: Remove broken alias routes from backend routers
STEP 3: Fix all portal pages to call WITHOUT trailing slash
STEP 4: Add redirect:follow to all fetch calls in portal
STEP 5: Rebuild portal fresh
STEP 6: Restart both services
STEP 7: Verify every endpoint through browser
